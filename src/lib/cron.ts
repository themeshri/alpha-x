// Automatic scraping disabled - use the Scrape button in the UI to trigger scraping manually

export function startCronJobs() {
  console.log('✅ Cron jobs initialized (automatic scraping disabled)');
  // No automatic scraping - only manual scraping via the Scrape button
}
